/// Stub implementation for non-web platforms.
/// These do nothing on mobile/desktop.

void updateWebFavicon(String url) {
  // noop on non-web
}

void updateWebTitle(String title) {
  // noop on non-web
}
